apiVersion: v1
kind: Service
metadata:
  name: {{ include "applib.fullname" . }}
  labels:
    {{- include "applib.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "applib.selectorLabels" . | nindent 4 }}
